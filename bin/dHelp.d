/*
I. Interchange two equations.
II. Multiply one equation by a nonzero number.
III. Add a multiple of one equation to a different equation.
*/
void dHelp(int x, y)
{
     class Ps5New
     {
         void Count(x, y)(ref multiple)
         {
              x + y = multiple;

              auto x + y;
         }
     }
return x + y;
}

