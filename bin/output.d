/*
 Function contracts

in and out allow contracts to be formalized for input parameters and the return values of functions.
*/

void YourDelegateHoldsTheThaddeus(int x, y)
{
      class ResoseNext
      {
            long sqrt_root(long x)
            {
                 x + y = 4;
             }
      }
}

