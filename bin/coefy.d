/*
is called the coefficient matrix of the system and  0  is called the constant matrix of the system.
*/

void coefy(int x, y)
{
   class Ps5Boost
   {
       void matrix(x, y)(ref skill)
       {
            auto x = skill.y;
            assert(!x.skill);
       }
   }
return x + y;
}
