module questput.quest3;

struct quest3 {

    void minister(x, y, z)(ref service)
    {
        interface A3
        {
            void call(x, y, z)(ref handle, R3, f)
            {
                /*
                    Question 3.

                    Let f be a continuous real-valued function on R³. Suppose that for every
                    sphere S of radius 1, the integral of f(x, y, z) over the surface of S
                    equals 0. Must f(x, y, z) be identically 0?

                */


                R3(x) =  f(x[0], y[0], z[0]);
                R3(y) =  f(x[0], y[0], z[0]);
                R3(z) =  f(x[0], y[0], z[0]);
            }
        }
        interface B3
        {
            void call(x, y, z)(ref handle, R3, f)
            {
                /*
                    Question 3.

                    Let f be a continuous real-valued function on R³. Suppose that for every
                    sphere S of radius 1, the integral of f(x, y, z) over the surface of S
                    equals 0. Must f(x, y, z) be identically 0?

                */


                R3(x) =  f(x[0], y[0], z[0]);
                R3(y) =  f(x[0], y[0], z[0]);
                R3(z) =  f(x[0], y[0], z[0]);
            }
        }

        interface C3
        {
            void call(x, y, z)(ref handle, R3, f)
            {
                /*
                    Question 3.

                    Let f be a continuous real-valued function on R³. Suppose that for every
                    sphere S of radius 1, the integral of f(x, y, z) over the surface of S
                    equals 0. Must f(x, y, z) be identically 0?

                */


                R3(x) =  f(x[0], y[0], z[0]);
                R3(y) =  f(x[0], y[0], z[0]);
                R3(z) =  f(x[0], y[0], z[0]);
            }
        }

    }
}
