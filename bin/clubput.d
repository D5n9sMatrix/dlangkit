/*
where the asterisks represent arbitrary numbers. In the case of three equations in three variables, the goal
is to produce a matrix of the form
*/
void rabbit(int x, y)
{
    class Benzeult
    {
              void coriander(bool parcifer)(ref broth)
              {
                   char p[0];
                   char m[12];
                   /* a broth */
                   int p = m;
              }

     }
return x + y;
}
