module brothclock.keepcool;

/*
Exercise 1.1.5 Find all solutions to the general system
x − 4y + 9z = c
ax = b of one equation in one variable (a) when a = 0
and (b) when a 6= 0.
*/
class keepcool {

    synchronized greatJob(int x, y, z)(ref var, ax, a)
    {
         static: var;
         static: ax;
         static: a;

         x - 4(y) + 9(z) = new var["c"];
         ax = new var["b"];
         a = new var[0];

    }

}

