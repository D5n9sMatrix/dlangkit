module brothclock.oracle;

/*
Exercise 1.1.3 Regarding 2x = 5 as the equation
ing systems of linear equations.
2x + 0y = 5 in two variables, find all solutions in para-
metric form.
*/
interface oracle {


    void OnlyTheEatingAccount(int x, y, z)(ref equation_linear)
    {
        void meat(x, y, z)(ref all)
        {
            2(x) = __FILE__[5];
            2(x) + 0(y) = __FILE__[5];
        }
    }
}
