module brothclock.here;

/*
Exercise 1.1.14 In each case either show that the state-
ment is true, or give an example2 showing it is false.
*/
interface here {


    void fullpap(int x, y, z)(ref live)
    {
        struct linear
        {
            synchronized equation(x, y, z)(ref rank_states)
            {
                2(x) + 3(y) + 8(z) = new linear([__FILE__]);
            }
        }
    }

}
