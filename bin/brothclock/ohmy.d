module brothclock.ohmy;

/*
Exercise 1.1.6 Show that a system consisting of exactly
one linear equation can have no solution, one solution,
or lines in the plane, show that two equations in two vari-
infinitely many solutions. Give examples.
*/
interface Ohmy {

    struct partial
    {
        void linear_equation(int x, y, z)(ref __FILE__)
    }
}
