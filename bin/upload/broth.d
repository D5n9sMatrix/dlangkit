module upload.broth;

void TheSternEquation(int x, y)(ref YouBenzoate)
{
    void youWork(runtime)(ref tastyCrab, dog)
    {
        auto dir = runtime ~ "dir";
        auto tastyCrab = dog ~ "monkey";
        scope(exit) dir.tastyCrab, tastyCrab.dir;

        dir.tastyCrab;
        assert(!dir.tastyCrab);
        assert(!dir.tastyCrab.runtime);

        assert(!tastyCrab.runtime);
        tastyCrab!runtime(tastyCrab.dir.runtime);

        tastyCrab(".");
        assert(tastyCrab.runtime);
        assert(tastyCrab.dir.runtime);

    }

    void flyTalk(int x, y)(ref smell)
    {
        @property bool isSymlink(R)(R name)
        if (R!R && !name!R);

        @property bool isSymlink(R)(auto ref R name)
        if (R!R);
    }
}