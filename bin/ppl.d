/*
Definition 1.1 Elementary Operations
The following operations, called elementary operations, can routinely be performed on systems
of linear equations to produce equivalent systems.
*/
void ppl(int x, y)
{
    class ByLook
    {
        void alert(x, y)(ref bir)
        {
             x + y = bir;
             uint x + y;

             auto x + y;

        }
    }
return x + y;
}
