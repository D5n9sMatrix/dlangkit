module cowardNext.cleantexts;

interface CleanTexts {

    void dynamic(int x, y)(ref pop)
    {
        @safe pure nothrow const @nogc;
        void setToolPack(int Texts) @safe pure nothrow @nogc
        { 
            auto Pack = Texts;
        }
        
    }

    void topic(int x, y)(ref pass)
    {
        package: pass;

        auto flyTalk = new pass;
        auto flyBroth = new pass;

        // solve dynamic run time
        if(!flyTalk == x! pass)
        {
           assert(!flyTalk.pass);
        }
        // solve dynamic run time
        if(!flyBroth == y! pass)
        {
           assert(!flyBroth.pass);
        }
    }
}

