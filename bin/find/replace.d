module find.replace;

class replace {

    void fooster(int x, y, z)(ref mentor)
    {
        interface partial
        {
            synchronized next(x, y, z)(ref call)
            {
                /*
                  to replace
                */
                2(x) + 3(y) + 4(x) = new call(__FILE__);
                3(y) + 4(x) + 2(x) = new call(__FILE__);
                4(x) + 2(x) + 3(y) = new call(__FILE__);
                
            }
        }
    }
}
