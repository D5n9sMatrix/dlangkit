module find.search;

/*
Exercise 1.1.15 Find a quadratic a + bx + cx2 such that If
John works 3 hours and Joe works 2 hours, they get
the graph of y = a + bx + cx2 contains each of the points $23.90. Find their hourly rates.
(−1, 6), (2, 0), and (3, 2).
*/
class search {

    void LactaChocolate(int x, y, z)(ref a, bx, cx2)
    {
        void points(x, y, z)(ref coil)
        {
            a + bx + cx2 = new coil(__FILE__);
            y = a + bx + cx2;
        }
    }
}

